<%= form_for @event, :url => {:controller => 'events', :action => 'update', :id => @event} do |f| %>
	<%= f.label :name, 'Name' %>
	<%= render :partial => 'form', :locals => {:f => f} %> 
	<%= f.submit 'Update' %>
<% end %>
